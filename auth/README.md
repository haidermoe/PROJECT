# 📁 مجلد إدارة المستخدمين (Authentication & User Management)

هذا المجلد يحتوي على جميع الملفات المتعلقة بإدارة المستخدمين والمصادقة.

## 📋 الملفات الموجودة:

### 🔐 ملفات المصادقة الأساسية:
- **`authController.js`** - معالجة طلبات المصادقة وإدارة المستخدمين
- **`authMiddleware.js`** - middleware للتحقق من التوكن والصلاحيات
- **`authRoutes.js`** - مسارات API للمصادقة
- **`auth_db.sql`** - ملف إنشاء قاعدة بيانات المستخدمين

### 🛠️ سكريبتات مساعدة:
- **`create_first_admin.js`** - إنشاء حساب مدير أولي
  ```bash
  node auth/create_first_admin.js
  ```

- **`create_kitchen_manager.js`** - إنشاء حساب مدير مطبخ
  ```bash
  node auth/create_kitchen_manager.js
  ```

- **`create_user.js`** - إنشاء حساب مستخدم (مرن - يمكنك اختيار الرتبة)
  ```bash
  node auth/create_user.js
  ```
  ⚙️ غيّر القيم في الملف قبل التشغيل (username, password, role, full_name)

- **`make_hash.js`** - تشفير كلمة مرور باستخدام bcrypt
  ```bash
  node auth/make_hash.js
  ```

### 📄 ملفات SQL:
- **`create_admin.sql`** - إنشاء حساب مدير مباشرة من SQL
- **`fix_users_table.sql`** - إصلاح جدول users (إضافة عمود full_name)

## 🔑 الرتب المتاحة:
- `admin` - مدير عام (صلاحيات كاملة)
- `manager` - مدير
- `kitchen_manager` - مدير مطبخ (يمكنه إدارة الوصفات)
- `employee` - موظف (صلاحيات محدودة)

## 📝 ملاحظات:
- جميع كلمات المرور مشفرة باستخدام `bcrypt` (10 rounds)
- يجب تغيير كلمات المرور الافتراضية بعد أول تسجيل دخول
- تأكد من وجود متغيرات البيئة في ملف `.env`:
  - `AUTH_DB_HOST`
  - `AUTH_DB_USER`
  - `AUTH_DB_PASSWORD`
  - `AUTH_DB_NAME`
  - `JWT_SECRET`

