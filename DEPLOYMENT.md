# 🚀 دليل النشر - Deployment Guide

دليل شامل لنشر نظام إدارة مخزون المطبخ على منصات مختلفة.

## 📋 المحتويات

1. [النشر على Render (مجاني)](#النشر-على-render)
2. [النشر على Railway (مجاني)](#النشر-على-railway)
3. [إعداد قاعدة البيانات للإنتاج](#إعداد-قاعدة-البيانات)
4. [نصائح الأمان](#نصائح-الأمان)

---

## 🌐 النشر على Render

### الخطوة 1: إعداد GitHub Repository

تأكد من أن الكود محدث على GitHub:

```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

### الخطوة 2: إنشاء حساب Render

1. اذهب إلى [render.com](https://render.com)
2. سجّل حساب جديد (مجاني)
3. اربط حساب GitHub

### الخطوة 3: إنشاء Web Service

1. اضغط **"New +"** → **"Web Service"**
2. اختر المستودع من GitHub
3. املأ المعلومات:
   - **Name**: `kitchen-inventory`
   - **Environment**: `Node`
   - **Region**: اختر الأقرب إليك
   - **Branch**: `main`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: `Free`

### الخطوة 4: إعداد قاعدة البيانات

⚠️ **ملاحظة مهمة**: Render لا يوفر MySQL في الخطة المجانية، فقط PostgreSQL.

**الخيارات المتاحة**:

#### الخيار 1: استخدام خدمة MySQL خارجية (موصى به)

استخدم أحد الخدمات التالية:

- **PlanetScale** (مجاني): [planetscale.com](https://planetscale.com)
- **Aiven** (مجاني للبدء): [aiven.io](https://aiven.io)
- **Clever Cloud** (مجاني للبدء): [clever-cloud.com](https://clever-cloud.com)

#### الخيار 2: استخدام PostgreSQL (يتطلب تعديل الكود)

يتطلب تغيير جميع استعلامات MySQL إلى PostgreSQL.

### الخطوة 5: إعداد متغيرات البيئة

في صفحة Web Service، اذهب إلى **"Environment"** وأضف:

```env
PORT=10000
BASE_URL=https://your-app-name.onrender.com
CORS_ORIGIN=*

# Auth Database
AUTH_DB_HOST=your-mysql-host.com
AUTH_DB_PORT=3306
AUTH_DB_USER=your-user
AUTH_DB_PASSWORD=your-password
AUTH_DB_NAME=auth_db

# App Database
DB_HOST=your-mysql-host.com
DB_PORT=3306
DB_USER=your-user
DB_PASSWORD=your-password
DB_NAME=kitchen_inventory

# JWT
JWT_SECRET=your-very-strong-secret-key-minimum-32-characters
```

### الخطوة 6: تهيئة قواعد البيانات

بعد إنشاء قواعد البيانات، قم بتشغيل SQL scripts:

```sql
-- على قاعدة auth_db
SOURCE auth/auth_db.sql;

-- على قاعدة kitchen_inventory
SOURCE init.sql;
```

### الخطوة 7: النشر

1. اضغط **"Manual Deploy"**
2. انتظر حتى يكتمل النشر
3. افتح الرابط العام

---

## 🚂 النشر على Railway

### الخطوة 1: إعداد Railway

1. اذهب إلى [railway.app](https://railway.app)
2. سجّل حساب جديد
3. اربط حساب GitHub

### الخطوة 2: إنشاء مشروع جديد

1. اضغط **"New Project"**
2. اختر **"Deploy from GitHub repo"**
3. اختر المستودع

### الخطوة 3: إضافة قاعدة بيانات MySQL

1. في المشروع، اضغط **"+ New"** → **"Database"** → **"MySQL"**
2. اختر الخطة المجانية
3. كرر الخطوة لإنشاء قاعدة بيانات ثانية

### الخطوة 4: إعداد متغيرات البيئة

1. اضغط على Web Service
2. اذهب إلى **"Variables"**
3. أضف المتغيرات من `.env.example`

### الخطوة 5: ربط قواعد البيانات

Railway سيقوم تلقائياً بإضافة متغيرات البيئة للقواعد.
استخدمها في إعدادات التطبيق.

---

## 🗄️ إعداد قاعدة البيانات للإنتاج

### إنشاء قواعد البيانات

ستحتاج إلى قاعدتين منفصلتين:

1. **auth_db** - للمصادقة
2. **kitchen_inventory** - للتطبيق

### تهيئة البيانات

بعد إنشاء القواعد، قم بتشغيل:

```bash
# على auth_db
mysql -h your-host -u your-user -p auth_db < auth/auth_db.sql

# على kitchen_inventory
mysql -h your-host -u your-user -p kitchen_inventory < init.sql
```

### إنشاء حساب مدير

بعد تهيئة قاعدة البيانات، أنشئ حساب مدير:

```bash
# محلياً (تأكد من إعدادات قاعدة البيانات في .env)
node auth/create_first_admin.js
```

أو يدوياً في قاعدة البيانات:

```sql
INSERT INTO users (username, password, role, full_name, is_active) 
VALUES ('admin', '$2b$10$hashed_password_here', 'admin', 'Admin User', 1);
```

---

## 🔒 نصائح الأمان

### 1. JWT Secret

استخدم مفتاح قوي وطويل:

```bash
openssl rand -base64 32
```

### 2. كلمات مرور قواعد البيانات

استخدم كلمات مرور قوية ومعقدة.

### 3. HTTPS

جميع منصات النشر توفر HTTPS تلقائياً.

### 4. متغيرات البيئة

- ✅ لا تشارك ملف `.env`
- ✅ استخدم Environment Variables في منصة النشر
- ✅ استخدم Secrets للمعلومات الحساسة

### 5. قاعدة البيانات

- ✅ قم بإنشاء backups منتظمة
- ✅ استخدم اتصالات مشفرة (SSL)
- ✅ قلل الصلاحيات للمستخدم إلى الحد الأدنى

---

## 📞 الدعم

للمساعدة، افتح Issue في GitHub: [Issues](https://github.com/haidermoe/PROJECT/issues)

---

**آخر تحديث**: 2024

