# 📄 تعليمات تحويل الفلو جارت إلى PDF بحجم A3

## الطريقة 1: استخدام السكريبت التلقائي (موصى به)

### الخطوات:

1. **تثبيت المكتبات** (إذا لم تكن مثبتة):
   ```bash
   npm install puppeteer --save-dev
   ```

2. **تشغيل السكريبت**:
   ```bash
   node generate-flowchart-pdf.js
   ```

3. **النتيجة**:
   - سيتم إنشاء ملف `FLOWCHART_A3.pdf` في المجلد الرئيسي
   - الحجم: A3 Landscape (420mm × 297mm)
   - يحتوي على جميع المخططات بوضوح

---

## الطريقة 2: استخدام المتصفح يدوياً

### الخطوات:

1. **فتح ملف HTML**:
   - افتح ملف `flowchart-to-pdf.html` في المتصفح (Chrome أو Edge)

2. **الطباعة إلى PDF**:
   - اضغط `Ctrl + P` (أو `Cmd + P` على Mac)
   - اختر "Save as PDF" كطابعة
   - في إعدادات الصفحة:
     - **Paper size**: A3
     - **Orientation**: Landscape (أفقي)
     - **Margins**: Custom (10mm)
   - اضغط "Save"

---

## الطريقة 3: استخدام أدوات أخرى

### باستخدام Chrome Headless:
```bash
chrome --headless --disable-gpu --print-to-pdf=FLOWCHART_A3.pdf --print-to-pdf-no-header flowchart-to-pdf.html
```

### باستخدام wkhtmltopdf:
```bash
wkhtmltopdf --page-size A3 --orientation Landscape flowchart-to-pdf.html FLOWCHART_A3.pdf
```

---

## ملاحظات مهمة:

1. **الانتظار**: عند استخدام السكريبت، قد يستغرق الأمر بضع دقائق لرسم جميع المخططات
2. **الحجم**: ملف PDF الناتج قد يكون كبيراً (10-20 MB) بسبب المخططات
3. **الوضوح**: جميع المخططات ستكون واضحة ومرئية في PDF
4. **الصفحات**: كل مخطط في صفحة منفصلة

---

## استكشاف الأخطاء:

### المشكلة: السكريبت لا يعمل
- تأكد من تثبيت Node.js
- تأكد من تثبيت puppeteer: `npm install puppeteer --save-dev`

### المشكلة: المخططات لا تظهر في PDF
- افتح ملف HTML في المتصفح أولاً للتحقق من أن المخططات تظهر
- انتظر بضع ثوانٍ إضافية بعد تحميل الصفحة

### المشكلة: PDF كبير جداً
- هذا طبيعي بسبب المخططات المعقدة
- يمكنك ضغط PDF باستخدام أدوات مثل Adobe Acrobat

---

**تم إنشاء هذا الدليل بواسطة:** AI Assistant  
**التاريخ:** 2024



