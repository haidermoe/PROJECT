# 🔧 إصلاح خطأ 500 في إنتاج الوصفات

## المشكلة
عند محاولة إنتاج وصفة، يظهر خطأ 500 في السيرفر.

## السبب
الجدول `recipe_productions` غير موجود في قاعدة البيانات.

## الحل

### الطريقة 1: إنشاء الجداول تلقائياً (موصى به)

الكود تم تحديثه ليقوم بإنشاء الجداول تلقائياً عند المحاولة الأولى. فقط أعد المحاولة!

### الطريقة 2: إنشاء الجداول يدوياً

إذا لم تعمل الطريقة التلقائية، قم بتنفيذ ملف SQL التالي:

```bash
mysql -u root -p kitchen_inventory < database/sql/create_production_tables.sql
```

أو من MySQL Workbench:
1. افتح الملف: `database/sql/create_production_tables.sql`
2. نفذ المحتوى

## الجداول المطلوبة

1. **recipe_productions** - جدول إنتاج الوصفات
2. **production_statistics** - جدول إحصائيات الإنتاج

## التحقق من الحل

بعد إنشاء الجداول، حاول إنتاج وصفة مرة أخرى. يجب أن تعمل بدون أخطاء.

## ملاحظة

إذا استمر الخطأ، تحقق من:
1. أن قاعدة البيانات `kitchen_inventory` موجودة
2. أن المستخدم لديه صلاحيات CREATE TABLE
3. أن الجداول تم إنشاؤها: `SHOW TABLES LIKE 'recipe_productions';`

