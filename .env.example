# ============================================
# Kitchen Inventory Management System
# Environment Variables Configuration
# ============================================

# Server Configuration
PORT=3000

# Application Base URL (for production deployment)
# Example: https://your-app-name.onrender.com
BASE_URL=http://localhost:3000

# CORS Configuration (for production, set to your frontend URL)
CORS_ORIGIN=*

# ============================================
# Authentication Database Configuration
# ============================================
# These settings are for the isolated authentication database (auth_db)
AUTH_DB_HOST=localhost
AUTH_DB_PORT=3306
AUTH_DB_USER=root
AUTH_DB_PASSWORD=your_auth_db_password_here
AUTH_DB_NAME=auth_db
AUTH_DB_CONNECTION_LIMIT=10

# ============================================
# Application Database Configuration
# ============================================
# These settings are for the main application database (kitchen_inventory)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_app_db_password_here
DB_NAME=kitchen_inventory
DB_CONNECTION_LIMIT=20

# ============================================
# JWT Authentication Configuration
# ============================================
# Generate a strong random secret (e.g., use: openssl rand -base64 32)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=30d

# ============================================
# Production Notes:
# ============================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder values with your actual credentials
# 3. NEVER commit .env to version control
# 4. For production hosting (Render, Railway, etc.), set these as environment variables
# 5. For managed databases, use the connection details provided by your hosting provider
