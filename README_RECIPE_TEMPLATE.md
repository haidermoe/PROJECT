# 📋 Standard Recipe Template - دليل الاستخدام

## ✅ ما تم إنجازه:

### 1. **تحديث قاعدة البيانات**
- تم إضافة الحقول التالية لجدول `recipes`:
  - `item_name` - اسم الوصفة
  - `yield` - الكمية المنتجة
  - `portions` - عدد الحصص
  - `shelf_life` - مدة الصلاحية
  - `procedure` - خطوات التحضير
  - `reference` - المرجع
  - `version` - الإصدار
  - `edition` - الطبعة

- تم إنشاء جدول `recipe_ingredients_new`:
  - `id` - المعرف
  - `recipe_id` - معرف الوصفة
  - `ingredient_name` - اسم المكون
  - `quantity` - الكمية
  - `display_order` - ترتيب العرض

### 2. **صفحة إضافة الوصفة**
- ✅ نموذج كامل مع جميع الحقول المطلوبة
- ✅ جدول ديناميكي لإضافة المكونات
- ✅ إمكانية إضافة/حذف مكونات
- ✅ خانة نصية لخطوات التحضير
- ✅ التحقق من البيانات قبل الحفظ

### 3. **صفحة عرض الوصفة (Recipe Viewer)**
- ✅ تصميم مطابق للورقة الأصلية
- ✅ عرض جميع البيانات بشكل منظم
- ✅ جدول المكونات والكميات
- ✅ قسم خطوات التحضير
- ✅ قابل للطباعة (A4)
- ✅ أزرار للطباعة والرجوع

### 4. **Backend APIs**
- ✅ API لإضافة وصفة جديدة (`POST /api/recipes/add`)
- ✅ API لجلب وصفة معينة (`GET /api/recipes/:id`)
- ✅ معالجة المكونات بشكل منفصل
- ✅ استخدام Transactions لضمان سلامة البيانات

---

## 🚀 كيفية الاستخدام:

### 1. تحديث قاعدة البيانات:

**الطريقة الأولى (مبسطة - موصى بها):**
```bash
mysql -u root -p kitchen_inventory < database_updates_recipe_template_simple.sql
```

**الطريقة الثانية (مع التحقق التلقائي):**
```bash
mysql -u root -p kitchen_inventory < database_updates_recipe_template.sql
```

أو شغّل الملف مباشرة في MySQL Workbench.

**ملاحظة:** إذا ظهر خطأ "Duplicate column name" في النسخة المبسطة، فهذا يعني أن العمود موجود بالفعل - يمكنك تجاهل الخطأ والمتابعة.

### 2. إضافة وصفة جديدة:
1. افتح صفحة الوصفات (`/recipes.html`)
2. اضغط على "➕ إضافة وصفة"
3. املأ البيانات:
   - اسم الوصفة (ITEM NAME) *
   - Yield *
   - Portions (اختياري)
   - Shelf Life (اختياري)
   - Reference (اختياري)
   - Version (افتراضي: 001)
   - Edition (افتراضي: 1)
4. أضف المكونات:
   - اضغط "➕ إضافة مكون"
   - اكتب اسم المكون والكمية
   - يمكنك إضافة عدة مكونات
5. اكتب خطوات التحضير *
6. اضغط "حفظ الوصفة"

### 3. عرض الوصفة:
- من جدول الوصفات، اضغط على زر "👁️ عرض"
- أو افتح `/recipe-viewer.html?id=1` (استبدل 1 بمعرف الوصفة)

---

## 📁 الملفات الجديدة/المحدثة:

### قاعدة البيانات:
- `database_updates_recipe_template.sql` - تحديثات قاعدة البيانات

### Frontend:
- `LOGIN/public/recipes.html` - تحديث نموذج الإضافة
- `LOGIN/public/recipes.js` - تحديث JavaScript
- `LOGIN/public/recipes.css` - تحديث CSS
- `LOGIN/public/recipe-viewer.html` - صفحة عرض الوصفة (جديد)
- `LOGIN/public/recipe-viewer.css` - CSS لعرض الوصفة (جديد)
- `LOGIN/public/recipe-viewer.js` - JavaScript لعرض الوصفة (جديد)

### Backend:
- `api/recipesController.js` - تحديث APIs
- `server.js` - إضافة مسار صفحة العرض

---

## 🎨 المميزات:

1. **تصميم مطابق للورقة الأصلية**
   - نفس التنسيق والخطوط
   - جداول منظمة
   - قابل للطباعة

2. **سهولة الاستخدام**
   - واجهة بسيطة وواضحة
   - إضافة/حذف مكونات بسهولة
   - التحقق من البيانات تلقائياً

3. **الأمان**
   - التحقق من الصلاحيات
   - معالجة آمنة للبيانات
   - استخدام Transactions

---

## 📝 ملاحظات:

- الحقول المطلوبة: `item_name`, `yield`, `procedure`, و مكون واحد على الأقل
- بعد حفظ الوصفة، سيتم توجيهك تلقائياً لصفحة العرض
- يمكنك طباعة الوصفة مباشرة من صفحة العرض (Ctrl+P)
- جميع الوصفات محفوظة في قاعدة البيانات ويمكن الوصول إليها لاحقاً

---

## 🔧 استكشاف الأخطاء:

إذا واجهت مشاكل:

1. **تأكد من تحديث قاعدة البيانات**:
   ```bash
   mysql -u root -p kitchen_inventory < database_updates_recipe_template.sql
   ```

2. **تحقق من Console المتصفح** (F12) لرؤية الأخطاء

3. **تحقق من Terminal السيرفر** لرؤية أخطاء Backend

4. **تأكد من وجود التوكن** في localStorage

---

## ✨ جاهز للاستخدام!

الآن يمكنك إضافة وعرض الوصفات بنفس تصميم الورقة الأصلية! 🎉

