# 📋 خطوات بسيطة - اتبعها بالترتيب

## ✅ الخطوة 1: افتح Railway Dashboard

1. اذهب إلى: https://railway.app
2. سجّل دخول بحساب GitHub
3. ستجد مشروعك "harmonious-playfulness" أو اسم مشابه

---

## ✅ الخطوة 2: أضف قاعدة بيانات MySQL

1. في Railway Dashboard
2. اضغط على **"+ New"** (في الأعلى)
3. اختر **"Database"**
4. اختر **"Add MySQL"**
5. انتظر قليلاً حتى يتم الإنشاء (30 ثانية تقريباً)

---

## ✅ الخطوة 3: انسخ معلومات قاعدة البيانات

1. اضغط على قاعدة البيانات التي أنشأتها (ستجدها في القائمة)
2. اذهب إلى **"Variables"** (في الأعلى)
3. ستجد متغيرات مثل:
   - `MYSQLHOST`
   - `MYSQLUSER`
   - `MYSQLPASSWORD`
   - `MYSQLPORT`
4. **انسخ هذه القيم** (ستحتاجها في الخطوة التالية)

---

## ✅ الخطوة 4: أضف متغيرات البيئة للمشروع

1. اضغط على مشروعك **"web"** (ليس قاعدة البيانات)
2. اذهب إلى **"Settings"** (في الأعلى)
3. اضغط على **"Variables"**
4. اضغط **"+ New Variable"**
5. أضف المتغيرات التالية **واحدة تلو الأخرى**:

### المتغير الأول:
- **Name:** `DB_HOST`
- **Value:** الصق قيمة `MYSQLHOST` من قاعدة البيانات
- اضغط **"Add"**

### المتغير الثاني:
- **Name:** `DB_USER`
- **Value:** الصق قيمة `MYSQLUSER` من قاعدة البيانات
- اضغط **"Add"**

### المتغير الثالث:
- **Name:** `DB_PASSWORD`
- **Value:** الصق قيمة `MYSQLPASSWORD` من قاعدة البيانات
- اضغط **"Add"**

### المتغير الرابع:
- **Name:** `DB_NAME`
- **Value:** `kitchen_inventory`
- اضغط **"Add"**

### المتغير الخامس:
- **Name:** `AUTH_DB_NAME`
- **Value:** `auth_db`
- اضغط **"Add"**

### المتغير السادس (JWT_SECRET):
- **Name:** `JWT_SECRET`
- **Value:** أنشئ مفتاح عشوائي (32 حرف على الأقل)
  - يمكنك استخدام: https://randomkeygen.com
  - أو انسخ هذا: `MySecretKey12345678901234567890`
- اضغط **"Add"**

### المتغير السابع:
- **Name:** `PORT`
- **Value:** `3000`
- اضغط **"Add"**

### المتغير الثامن:
- **Name:** `NODE_ENV`
- **Value:** `production`
- اضغط **"Add"**

---

## ✅ الخطوة 5: انتظر إعادة التشغيل

- بعد إضافة المتغيرات، Railway سيعيد تشغيل المشروع تلقائياً
- انتظر حتى ترى "Active" بدلاً من "INITIALIZING"
- قد يستغرق 1-2 دقيقة

---

## ✅ الخطوة 6: افتح Terminal في Railway

1. اضغط على مشروعك **"web"**
2. اذهب إلى **"Deployments"** (في الأعلى)
3. اضغط على آخر deployment (الأول في القائمة)
4. اضغط **"View Logs"**
5. في الأعلى، ستجد **"Shell"** أو **"Terminal"** - اضغط عليه

---

## ✅ الخطوة 7: شغّل سكريبت الإعداد

في Terminal الذي فتحته، اكتب:

```bash
node railway-setup.js
```

ثم اضغط Enter

- سيقوم بإنشاء قواعد البيانات والجداول تلقائياً
- قد يستغرق 30-60 ثانية

---

## ✅ الخطوة 8: أنشئ حساب مدير

في نفس Terminal، اكتب:

```bash
node auth/create_first_admin.js
```

ثم اضغط Enter

- سيطلب منك:
  - اسم المستخدم (مثلاً: `admin`)
  - كلمة المرور (مثلاً: `admin123`)
  - الاسم الكامل (مثلاً: `مدير النظام`)

---

## ✅ الخطوة 9: افتح المشروع

1. في Railway Dashboard
2. اضغط على مشروعك **"web"**
3. ستجد رابط مثل: `web-production-xxxx.up.railway.app`
4. اضغط على الرابط أو انسخه وافتحه في المتصفح
5. يجب أن تظهر صفحة تسجيل الدخول
6. سجّل دخول بالحساب الذي أنشأته

---

## 🎉 تم! المشروع جاهز

إذا واجهت أي مشكلة، راجع قسم "حل المشاكل" أدناه.

---

## ❓ حل المشاكل

### المشكلة: "Cannot connect to database"
**الحل:**
- تأكد من أنك نسخت `MYSQLHOST` و `MYSQLUSER` و `MYSQLPASSWORD` بشكل صحيح
- تأكد من أن قاعدة البيانات تعمل (يجب أن تراها في القائمة)

### المشكلة: "JWT_SECRET is required"
**الحل:**
- تأكد من إضافة متغير `JWT_SECRET` في Variables
- استخدم مفتاح عشوائي طويل (32 حرف على الأقل)

### المشكلة: "Port already in use"
**الحل:**
- تأكد من إضافة متغير `PORT=3000` في Variables
- Railway سيستخدمه تلقائياً

### المشكلة: Terminal لا يعمل
**الحل:**
- انتظر قليلاً حتى يكتمل النشر
- جرب إعادة تحميل الصفحة
- تأكد من أن المشروع في حالة "Active"

---

## 📞 تحتاج مساعدة؟

إذا واجهت مشكلة في أي خطوة:
1. التقط صورة للشاشة
2. أو اكتب رسالة توضح المشكلة
3. سأساعدك في حلها

